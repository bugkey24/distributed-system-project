-- Database initialization script
CREATE DATABASE IF NOT EXISTS distributed_db;
USE distributed_db;

-- The table will be created via Laravel migrations, 
-- but we can put any initial seeds or setup here if required.
